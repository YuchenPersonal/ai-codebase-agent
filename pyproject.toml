[project]
name = "ai-codebase-agent"
version = "0.1.0"
description = "An AI agent for analyzing codebases"
requires-python = ">=3.14"

dependencies = []

[project.optional-dependencies]
dev = [
    "pytest",
]

[project.scripts]
ai-codebase-analyze = "ai_codebase_analyzer.cli:main"